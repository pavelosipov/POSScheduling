source 'https://github.com/CocoaPods/Specs.git'

platform :ios, '7.0'
workspace 'POSSchedulableObject'

target "POSSchedulableObject" do
end

target "Tests" do
    pod 'POSSchedulableObject', :path => '.'
    pod 'POSAllocationTracker'
end
